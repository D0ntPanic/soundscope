uniform sampler2D image;

void main(void)
{
	float sum = 0.0;
	for (int i = 0; i < 8; i++)
		sum += texture2D(image, gl_TexCoord[0].xy + vec2((float(i) - 3.5) / 768.0, 0)).r;
	float avg = sum * 0.125;
	gl_FragColor = vec4(avg, avg, avg, 1);
}

